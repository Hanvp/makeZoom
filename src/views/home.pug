doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
        header
            h1 Noom
        main
         div#enterRoom
            h2 [들어갈 수 있는 방]
            ul
            form
                input(placeholder = "room name", type="text", required)
                button Enter Room
        div#sendMessage
            h3
            ul
            form
                input(placeholder = "Wirte a message", type="text", required)
                button Send
        div#nickname
            form
                input(placeholder = "nicknmae", type="text", required)
                button Save
        script(src="/socket.io/socket.io.js") 
        script(src="/public/app.js") 
